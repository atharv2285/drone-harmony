# Vercel Environment Variables (Frontend Only)

# Backend API URL - point this to where your backend is hosted
# For now, you can use localhost for testing or deploy backend to Railway/Render
VITE_BACKEND_URL=http://localhost:3001

# Or if you deploy backend separately:
# VITE_BACKEND_URL=https://your-backend.railway.app
